
//determines what device we are on 
enum DeviceScreenType {
  @Deprecated('Use lowercase version')
  Mobile,
  @Deprecated('Use lowercase version')
  Tablet,
  @Deprecated('Use lowercase version')
  Desktop,
  @Deprecated('Use lowercase version')
  Watch,
  mobile,
  tablet,
  desktop,
  watch
}

//for every device there is a normal large and extra large size 
enum RefinedSize { normal, large, extraLarge }
